version: "3.9"

services:
  personal-ai-os:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./database:/app/database
      - ./uploads:/app/uploads
      - ./logs:/app/logs
      - ./backups:/app/backups
